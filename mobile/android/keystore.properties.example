# Copy to keystore.properties (gitignored) and fill in real values, OR set the
# equivalent FLEETWATCH_* environment variables in CI. Never commit real values.
storeFile=fleetwatch-release.keystore
storePassword=CHANGE_ME
keyAlias=fleetwatch
keyPassword=CHANGE_ME
